# ---- Xcode / Swift -------------------------------------------------------
xcuserdata/
DerivedData/
build/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# XcodeGen generates the project from project.yml; we commit the spec, not the
# generated project.
GlowUp.xcodeproj/

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved

# CocoaPods / Carthage (not used, ignored for safety)
Pods/
Carthage/Build/

# ---- macOS ---------------------------------------------------------------
.DS_Store
*.swp

# ---- ML pipeline (Python) ------------------------------------------------
ml/.venv/
**/__pycache__/
*.pyc
ml/datasets/
ml/runs/
ml/artifacts/

# Trained model artifacts are large; track via Git LFS or releases, not git.
*.mlmodel
*.mlpackage
*.pt
*.onnx
*.weights

# ---- Secrets / env -------------------------------------------------------
*.env
secrets.xcconfig
